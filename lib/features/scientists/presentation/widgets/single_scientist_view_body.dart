import 'package:flutter/widgets.dart';

class SingleScientistViewBody extends StatelessWidget {
  const SingleScientistViewBody({super.key});
  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
